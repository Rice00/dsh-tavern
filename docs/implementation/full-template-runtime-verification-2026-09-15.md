# 完整上游模板运行时接入验证

分支：`refactor/full-prompt-template`。
上游：ST-Prompt-Template 1.17.9，`d6f520d149aba146305b0b781ddd691d449c28d2`。

## 变更

删除自制 QuickJS/EJS 运行时及四个独立依赖，生产调用改为等待所属会话的浏览器上游实例。开场初始化使用临时实例和私有草稿；世界书投影执行上游 EJS/变量/世界书函数；最终原生前后台请求执行上游生成事件。兼容请求也使用同一整包入口。页面需保持在线，没有旧引擎回退。

世界书召回、十轮冷却、软预算及固定/动态位置保留现有逻辑。上游源码未修改，宿主适配代码独立存放。

## 验证

- 全量测试：2,195 项通过；随后模型名称适配的相关 38 项测试通过。
- 上游源码哈希/导入审计通过；客户端生成产物检查通过。
- 浏览器整包测试覆盖初始变量、完整 Lodash、YAML、聊天历史、嵌套世界书、主动激活、请求事件、工具和推理块保留。
- 本地正式服务，独立“命定之诗”测试存档，两轮前台/后台均完成，结算为 done，无模板诊断。
- 状态条目 97、骰子池 109、地图 141 实际输出正常。
- 两轮固定 system 内容逐字符一致，48,511 字符。
- 前台第一轮：未命中 2,841 / 缓存读取 148,736 token，命中率 98.13%。第二轮：2,802 / 151,936，98.19%。
- 最终请求事件接入后的后台：未命中 7,156 / 缓存读取 33,152，82.25%。数据来自原生 usage；前后台原生 turn 编号不相同，以请求所属游戏轮关联。
- 新开场准备页完成加载，卡片检查显示 EJS 已启用、MVU 正常，开场选择及开始按钮可用；未创建额外正式游戏。

## 范围

这是上游整包进入生产执行链路的验证，不能等同全部 SillyTavern 宿主功能兼容。回复 DOM 渲染生命周期、上游设置界面及依赖其他扩展的 API 尚需单独接入。当前不支持的宿主能力明确报错。

完整请求/存档证据仅留本机 `output/worldbook-live-2026-09-15/full-upstream-*`，不提交人物卡正文和私人存档。

## 生命周期、编辑器与命令接入

已增加官方消息渲染、永久改写、聊天/世界书更新与回复版本事件。玩家模板在召回前执行，待提交变量由原生 Chat 保存；生成结束时合入用户消息，重试复用结果。永久改写通过 Session surface replace 追加事件，保留消息中的非文本块。

上游设置与 Monaco 使用当前游戏的同一个实例，从 DSH Tavern 设置进入。`/ejs`、`/ejs-refresh` 注册到原生命令服务；模板内命令接入已注册的宿主命令。世界书编辑支持无 MVU 的纯模板卡。

验证：全量 2204 项通过；随后新增的原生/兼容输入流程、待提交变量和回复版本删除检查共 69 项通过。真实 Chromium 已验证 Monaco 加载、取消、保存及条目持久化；原生 Session 恢复后，真实 Agent 请求只含改写后的用户消息和回复。模型与缓存复测尚在进行。

固定上游差异：`evalTemplateWI` 对关键词使用正文求值，已在独立 SillyTavern 1.18.0 实例重现（预处理正文为“值 3”时，关键词也变成“值 3”）。模板异常不会撤销先前已经产生的激活副作用。保持固定上游源码原样。
