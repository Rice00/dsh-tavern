# 完整上游模板接入验证

分支：`refactor/full-prompt-template`。上游：ST-Prompt-Template 1.17.9，固定提交 `d6f520d149aba146305b0b781ddd691d449c28d2`。

## 实现与自动检查

- 自制 QuickJS/EJS 已移除；五个完整上游入口接入开场、玩家输入、世界书、原生/兼容前后台请求、消息显示、设置、Monaco 及命令。
- 原生 Chat/Journal 保存变量、swipe 和处理标记；永久正文改写使用 Session surface replace。真实 Session 磁盘恢复后的 Agent 请求只包含改写后的正文，非文本块保留。
- 浏览器测试覆盖初始变量、完整 Lodash/YAML、嵌套世界书、主动激活、官方生成/渲染事件、一次性输入求值、Monaco 保存/取消、Worker 编译以及隐藏镜像不执行卡片脚本。
- 模板命令发起生成后立即返回提交结果，避免等待占用同一个执行队列的生成任务。
- 原生命令 `/ejs` 声明自由输入参数，避免输入框将带参数命令当作聊天正文。
- 模板 iframe 自带标准库兼容补丁；测试删除浏览器的 `structuredClone` 和 `Array.prototype.at` 后再加载完整实例。
- 最终全量 2,209 项通过，包含命令参数回归及缺少现代标准库函数的浏览器环境检查。
- 上游源码哈希/导入审计与客户端构建产物检查通过。

## 真实 ST 对照

隔离 SillyTavern 1.18.0（`d7adfdb61`），构建相同固定版本上游源码：

| 场景 | ST 与 DSH 结果 |
| --- | --- |
| 永久正文模板及消息变量 | 正文“值 1”，count=1 |
| render_before | 显示前缀加入正文 HTML |
| 完整 Lodash keyBy | 返回 yes |
| `/ejs ctx={"x":3} block=true x \|\| 7` | 返回 3 |
| 世界书预处理 | 保留条目 1203，正文及关键词均为“值 3” |

固定上游的 `evalTemplateWI` 对关键词使用正文求值，以上异常关键词结果在真正 ST 中也能重现。模板异常不会撤销先前激活副作用。没有为了测试结果修改上游源码。

## 真实游戏与缓存

正式本地服务，独立“命定之诗”测试存档，DeepSeek-V4-Flash / Low。前台两轮回复与后台结算完成，无模板/世界书错误，待提交输入清理正常。状态条目、骰子池和地图已实际输出。

| 请求 | 未命中 token | 缓存读取 token | 命中率 |
| --- | ---: | ---: | ---: |
| 前台原生 turn 16 | 2,750 | 155,008 | 98.26% |
| 前台原生 turn 17 | 2,850 | 158,080 | 98.23% |
| 第二轮对应后台原生 turn 16 | 8,226 | 32,128 | 79.62% |

两轮前台实际请求的固定 system 完全相同，均为 48,511 字符。数据来自原生 usage，而非本地 token 估算。前后台原生 turn 编号不同。

完整请求和存档证据保留于本机 `output/worldbook-live-2026-09-15/`，不提交私人正文。

## 范围

接通完整 ST-Prompt-Template 插件，不等于安装了全部 ST 扩展。关键词召回、十轮冷却、软预算、固定/动态位置仍为 DSH Tavern 的领域规则。浏览器需保持游玩页面在线。

## 最终入口与 Android 检查

- 正式输入框执行 `/ejs block=true 3+4`，原生日志 `command/run` → `command/done`，返回 success / 7；没有新增模型请求。
- MuMu 现有 Android 12、Chrome/WebView 83 内核加载相同构建产物：完整上游初始化成功，Worker + Lodash 返回“值 7”，永久正文模板返回“值 1”并保存 n=1，再次同步不重复执行。
- Android 此项使用隔离浏览器 fixture 验证插件运行时；不是 DSHA 本地安装升级后的整场游戏验收。模拟器自带浏览器访问完整 DSH 页面还会遇到宿主缺少 `Promise.withResolvers` / `AbortSignal.any`，不计为通过。
- 独占配信卡未做本轮完整实测；真实模型证据来自命定之诗。没有将未执行的旧计划验收项目标成通过。
