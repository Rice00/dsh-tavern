# 命定之诗随机池前缀验证

原卡：本地导入命定之诗v4.3.1.json，未修改卡文件。entry:109 [随机池] 为常驻、对话内深度 2，包含骰子宏、性格随机宏及 Math.random 命名脚本。

修改前，同一状态连续执行正式 projectWorldBookTemplates，唯一变化条目为 entry:109，location=prefix，前缀长度约 32844 字符。骰子宏被渲染成字面文本 roll 1d20。

修改后用完整原卡重放不同轮次种子：固定前缀完全相同；同轮重复渲染全文完全相同；不同轮次 foregroundContext 不同；随机池 location=foreground；60 个骰子均实际展开，无 roll 1d20 残留。

回归测试覆盖固定前缀、随机位置前后包裹标签、同轮复用、下一轮刷新、后台同种子一致及常驻不消费冷却。未调用真实提供方测缓存比例，不将前缀稳定性等同于具体缓存命中率或费用保证。

后台任务同样分离固定前缀与每次任务追加内容。runner 回归检查连续任务系统前缀相同、各任务消息使用自己的骰子结果，旧消息不被后续结果替换。

## 实际提供方缓存用量

在 bdb5b35 本地服务中，用原卡新建独立存档 chat-mu29khb0-bd8pkw，DeepSeek 官方 deepseek-v4-flash / Low，通过真实浏览器连续三轮旅店观察、喝水行动，正文与结算全部完成。以下数据取自原生 assistant/chunk 的 usage 事件。

| 轮次 | 前台缓存读取 | 前台未命中输入 | 前台命中率 | 后台缓存读取 | 后台未命中输入 | 后台命中率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0 | 32482 | 0% | 1664 | 25943 | 6.0% |
| 2 | 33664 | 21118 | 61.5% | 14848 | 12775 | 53.8% |
| 3 | 55552 | 21143 | 72.4% | 14848 | 12799 | 53.7% |

统计口径核对运行时 dsh-llm-deepseek：inputTokens = prompt_tokens - cacheReadTokens，即未命中输入；命中率分母为 inputTokens + cacheReadTokens，不能直接除 inputTokens。

三轮前台 system 完全相同；随机池 location=foreground，骰子逐轮变化。后续请求保留此前随机池的文本。说明变动随机池与缓存读取能同时存在；这不是与旧版本做严格 A/B，也不据此承诺所有卡的命中比例。

当前为保护同位置标签顺序，随机条目所在位置的固定规则也随轮追加，前台每轮仍有约 2.1 万未命中输入 token，存在重复内容开销。不能把比例逐轮上升等同于绝对输入成本下降。

原始证据：output/worldbook-live-2026-09-15/cache-usage.json、cache-{one,two,three}-{requests,state,recall}.json，以及对应 cache-native-*.json；不纳入 Git。

## 固定条目重复追加修正

撤销“同位置整组追加”：逐条判定随机/非常驻，仅对源码中存在实际配对跨条目标签的区间整体迁移。同位置独立固定段留在前缀。回归覆盖嵌套角色库标签、前后独立固定段、随机条目、绿灯条目。

完整命定卡常驻投影重放：固定前缀 44564 字符，逐轮追加 4516 字符；随机池约 609 字符，其余追加部分为种族、势力、组织、冒险区域、角色等实际包裹绿灯条目的标签区间。不同种子下固定前缀一致。此数字是本地常驻投影字符数，不是模型 token 用量，也不含额外命中的非常驻正文。

前面的三轮缓存表来自修正前版本，不能作为这次修正后的实测缓存比例。

## 标签与固定正文拆分

独立纯标签边界可以同时用于前缀和本轮上下文，区间内固定概览仅保留在前缀。嵌套标签同样保持完整；混写正文的边界暂不拆分。

命定卡空会话常驻投影：前缀 48458 字符，本轮 678 字符（随机池 609 字符，其余为标签与分隔），不同种子下前缀一致。该重放缺少真实存档变量，条目 97、141 报模板运行错误，故这些数字仅代表此测试状态，不代表完整游戏请求或实际缓存命中率。

## 拆分后真实模型三轮复测

运行版本 a2ea3e0，本地正式服务，沿用独立测试存档 chat-mu29khb0-bd8pkw；DeepSeek 官方 deepseek-v4-flash，Low。继续原测试的安静旅店场景，三个正文请求及三个后台结算均 completed。

| 复测轮次 | 前台未命中 token | 前台缓存读取 token | 命中率 | 后台未命中 token | 后台缓存读取 token |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 98126 | 0 | 0% | 5786 | 1664 |
| 2 | 894 | 98432 | 99.1% | 5768 | 1664 |
| 3 | 915 | 99584 | 99.1% | 5779 | 1664 |

沿用存档包含旧版重复注入的历史。第一轮是修改前缀后的冷缓存请求，不能与后续热缓存请求混为一谈。后两轮未命中输入明显低于旧版本同存档约 2.1 万 token；这是顺序复测，不是严格控制变量的 A/B。

三个前台实际 request.system 完全一致，固定内容仍在前缀；三个随机池文本互不相同，每轮约 611 字符。

发现的未解决问题：

- 后台 actual request.system 只有 134 字符的后台角色指令。逐项检查召回日志中 24 个长度超过 100 字符的固定输出，全部未出现在后台 system 或 messages 中。后台低输入不能视为有效优化：缺失了固定世界书。旧版记录的后台 system 也只有 134 字符，之前部分内容因随轮追加而可见，此次移回前缀后暴露缺失。
- entry:97 状态模板与 entry:141 地图模板在真实存档中仍报 runtime-error。通过只读模板探针定位到运行时分别缺少 Lodash omit、keyBy；不是空会话缺变量。补齐首个缺失函数后仍需验证后续执行，不能假设只有这两个函数不支持。

证据：output/worldbook-live-2026-09-15/split-{one,two,three}-{state,requests,recall}.json、split-usage.json、split-native-*.json（本地保留，不入 Git）。
