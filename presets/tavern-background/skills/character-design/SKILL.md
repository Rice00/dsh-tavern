---
name: character-design
description: "用户手动发起后，由共享后台 Agent 为重要人物建立、补全或复用人物设计档案；所有人物卡都可使用。"
user-invocable: false
---

# 后台人物设计

你是当前后台 Agent，正在执行用户手动发起的人物设计任务。在同一个 Agent 会话内完成设计，不创建另一个 Agent。设计意见选填；未填写时，根据当前剧情和已有档案选择需要设计的重要人物。

人物档案适用于普通卡与 MVU 卡，只属于当前对话，不注入前台正文 Agent。人物设计独立保存，不属于姿势字段或 MVU operations；保存人物设计不会替代、跳过或自动完成当前任务的姿势与变量结算。

## 选择人物

调用 `character_design_read` 读取精简索引。优先复用或补充已有档案；只有重要、可能持续登场或值得提前储备、能够推动剧情的人物才建立档案。路人、一次性功能角色和没有明确剧情价值的备选人物保持轻量，不建档。剧情确实需要多名重要人物时，可以分别完整设计，不设固定数量上限。

索引中有可复用人物时，按索引中的人物姓名读取完整档案并优先补充。不因姓名写法、暂时离场或状态变化重复创建同一人物。

## 完整设计

档案只填写 6 项：身份、剧情定位、性格、外貌、说话方式、人物关系。外貌包含日常穿着；剧情定位包含人物作用和可能的发展方向。各项各写其重点，不重复同一内容。

结合题材、世界设定、现有人物和可能的剧情走向，使人物面对选择、冲突与关系变化时能够自然行动。人物之间保持鲜明差异；设计可能性，不预写已经发生的剧情。

所有可设计内容都给出具体值。人物方案中不使用“未明确”“未知”“待定”或同类占位词。

完成后调用 `character_design_save` 保存整份档案；它只接收人物设计字段，不接收变量路径或变量对象。工具拒绝时补全方案后重试。

## 完成任务

完成全部读取和保存后，调用 `character_design_finish` 结束设计。本次任务不提交姿势、变量或候选项。

人物设计不输出到回复文本，不生成 HTML，也不向前台追加设计说明。
