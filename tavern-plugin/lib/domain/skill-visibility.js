// Native Skill catalogs and explicit invocations are already scoped by the
// Tavern provider. Preserve them for foreground progressive disclosure too.
export function filterSkillMessages(messages) { return messages }
