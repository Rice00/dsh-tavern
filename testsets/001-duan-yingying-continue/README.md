# 001 段莹莹_自由 · 三轮候选行动

正式游戏 API 直接引用 `段莹莹_自由.json`，每次新开独立测试会话。
模型：`deepseek-official / deepseek-v4-flash / high`。
第一轮发送案例配置中的输入，后续采用上一轮第一个行动候选；记录正文、后台结算、候选和失败。

正式酒馆保存卡片修改后即可重新运行。无需导出、复制卡片或同步 Profile。依赖浏览器脚本的卡会明确报告当前 API 运行时不支持。此案例不包含卡片编辑与生图任务。
